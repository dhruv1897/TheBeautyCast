# Pinned so every developer and the server run the same versions.
# Update with care: change a version, run the tests, then commit.

Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.2.1
python-dotenv==1.0.1
gunicorn==22.0.0
pytest==8.2.2
